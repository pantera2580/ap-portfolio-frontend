import { ExperienceResponse } from './experience-response';

describe('ExperienceResponse', () => {
  it('should create an instance', () => {
    expect(new ExperienceResponse()).toBeTruthy();
  });
});
